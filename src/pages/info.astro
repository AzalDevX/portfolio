---
import MainLayout from '../layouts/Layout.astro';

const socialLinks = [
  { name: 'Portfolio', url: 'https://lalo.lol/me' },
  { name: 'GitHub', url: 'https://lalo.lol/gh' },
  { name: 'LinkedIn', url: 'https://lalo.lol/in' },
  { name: 'Twitter', url: 'https://lalo.lol/x' },
];

const services = [
  {
    title: 'Web Design',
    description: 'Custom solutions ideal for restaurants, yoga studios, fashion stores, and more.',
  },
  {
    title: 'SEO & Speed Optimization',
    description: 'All sites are optimized for search engines and fast performance.',
  },
  {
    title: 'Support & Updates',
    description: 'I provide ongoing support to keep your site secure and up-to-date.',
  },
];

const contactInfo = [
  { text: 'tunuevaweb.es', href: 'https://lalo.lol/tnw' },
  { text: 'contacto@tunuevaweb.es', href: 'mailto:contacto@tunuevaweb.es' },
  { text: '@tunuevawebya', href: 'https://instagram.com/tunuevawebya' },
  { text: '@tunuevaweb', href: 'https://www.tiktok.com/@tunuevaweb' },
];

const examples = [
  { name: 'Bite&Smash', href: 'https://bite-and-smash.tunuevaweb.es' },
  { name: 'Pulse Studio', href: 'https://pulse-studio.tunuevaweb.es' },
];
---

<MainLayout title="Info - Gonzalo Azaldegi">
  <div class="min-h-screen bg-white text-gray-900 font-light">
    <div class="max-w-4xl mx-auto px-8 pb-16 pt-32">
      
      <!-- Header -->
      <header class="text-center mb-20 fade-in">
        <h1 class="text-6xl md:text-7xl font-extralight mb-4 tracking-tighter">
          Gonzalo Azaldegi
        </h1>
        <p class="text-lg text-gray-600 mb-4 tracking-wide">
          Web Developer | Astro Specialist & UI/UX Designer
        </p>
        <p class="text-gray-500 max-w-2xl mx-auto leading-relaxed">
          I'm Gonzalo Azaldegi, a web developer specialized in modern frameworks like Astro and Vue. 
          I'm passionate about minimalist design and creating attractive, functional web experiences.
        </p>
      </header>

      <!-- Social Links -->
      <section class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4 mb-20">
        {socialLinks.map((link, index) => (
          <a 
            href={link.url} 
            target="_blank" 
            rel="noopener noreferrer"
            class="group flex items-center justify-between p-4 border border-gray-200 hover:border-gray-400 transition-colors duration-300 fade-in"
            style={`animation-delay: ${index * 100}ms`}
          >
            <span class="text-sm tracking-wide">{link.name}</span>
            <span class="text-gray-400 group-hover:text-gray-600 transition-colors duration-300">→</span>
          </a>
        ))}
      </section>

      <!-- TuNuevaWeb Section -->
      <section class="mb-20 fade-in">
        <div class="border-t border-gray-200 pt-16">
          <h2 class="text-3xl font-light mb-8 tracking-tight">
            TuNuevaWeb - Professional Services
          </h2>
          <p class="text-gray-600 mb-12 leading-relaxed text-lg">
            At TuNuevaWeb, I offer modern and functional websites for businesses of all sizes, 
            starting from very low cost. My sites will help you stand out in the digital space.
          </p>
          
          <!-- Services -->
          <div class="space-y-8 mb-12">
            {services.map((service, index) => (
              <div 
                class="group border-b border-gray-100 pb-6 fade-in"
                style={`animation-delay: ${index * 150}ms`}
              >
                <h3 class="text-lg font-medium mb-2 group-hover:text-gray-600 transition-colors duration-300">
                  {service.title}
                </h3>
                <p class="text-gray-500 text-sm leading-relaxed">
                  {service.description}
                </p>
              </div>
            ))}
          </div>

          <!-- Contact Info -->
          <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            {contactInfo.map((item, index) => (
              <a 
                href={item.href}
                class="text-sm text-gray-500 hover:text-gray-900 transition-colors duration-300 fade-in"
                style={`animation-delay: ${index * 100 + 300}ms`}
              >
                {item.text}
              </a>
            ))}
          </div>
        </div>
      </section>

      <!-- Examples Section -->
      <section class="mb-20 fade-in">
        <div class="border-t border-gray-200 pt-16">
          <p class="text-center text-gray-600 mb-8">
            You can see two examples of websites at
          </p>
          <div class="flex justify-center gap-8 flex-wrap">
            {examples.map((example, index) => (
              <a 
                href={example.href}
                target="_blank"
                rel="noopener noreferrer"
                class="group text-sm tracking-wide border-b border-gray-300 pb-1 hover:border-gray-900 transition-colors duration-300 fade-in"
                style={`animation-delay: ${index * 100}ms`}
              >
                <span class="group-hover:text-gray-600 transition-colors duration-300">
                  {example.name}
                </span>
              </a>
            ))}
          </div>
        </div>
      </section>

      <!-- Templates Section -->
      <section class="space-y-16 mb-20">
        <!-- Astro Template -->
        <div class="border-t border-gray-200 pt-16 fade-in">
          <h2 class="text-2xl font-light mb-6 tracking-tight">
            Astro Template
          </h2>
          <p class="text-gray-600 mb-8 leading-relaxed">
            Minimalist and modern template in Astro + TailwindCSS, ideal for quick and stylish projects. 
            Download and customize it.
          </p>
          <a 
            href="https://github.com/AzalDevX/astro-template"
            target="_blank"
            rel="noopener noreferrer"
            class="inline-block text-sm tracking-wide border-b border-gray-900 pb-1 hover:text-gray-600 transition-colors duration-300"
          >
            Download Template
          </a>
        </div>

        <!-- Vue Template -->
        <div class="border-t border-gray-200 pt-16 fade-in">
          <h2 class="text-2xl font-light mb-6 tracking-tight">
            Vue Template
          </h2>
          <p class="text-gray-600 mb-8 leading-relaxed">
            Minimalist and modern template for Vue 3 + Vite + TailwindCSS, designed for single-page applications (SPA). 
            Includes optimized initial configuration and support for complete customization.
          </p>
          <a 
            href="https://github.com/AzalDevX/vue-template"
            target="_blank"
            rel="noopener noreferrer"
            class="inline-block text-sm tracking-wide border-b border-gray-900 pb-1 hover:text-gray-600 transition-colors duration-300"
          >
            Download Template
          </a>
        </div>
      </section>

      <!-- Contact Button -->
      <div class="text-center mb-16 fade-in">
        <a 
          href="mailto:me@azaldev.com"
          class="inline-block text-lg tracking-wide border-b-2 border-gray-900 pb-2 hover:text-gray-600 hover:border-gray-600 transition-all duration-300"
        >
          Contact Me
        </a>
      </div>

      <!-- Blog Link -->
      <div class="text-center fade-in">
        <a 
          href="https://lalo.lol/blog"
          target="_blank"
          rel="noopener noreferrer"
          class="text-sm text-gray-500 hover:text-gray-900 transition-colors duration-300 tracking-wide"
        >
          Read my articles about web development
        </a>
      </div>

    </div>
  </div>
</MainLayout>

<style>
  .fade-in {
    opacity: 0;
    transform: translateY(20px);
    animation: fadeIn 0.6s ease-out forwards;
  }

  @keyframes fadeIn {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Staggered animation delays are handled inline */
</style>

<script>
  // Add intersection observer for fade-in animations
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.style.animationPlayState = 'running';
      }
    });
  }, observerOptions);

  // Observe all fade-in elements
  document.querySelectorAll('.fade-in').forEach(el => {
    el.style.animationPlayState = 'paused';
    observer.observe(el);
  });

  // Start first elements immediately
  setTimeout(() => {
    document.querySelectorAll('.fade-in').forEach((el, index) => {
      if (index < 3) { // First few elements
        el.style.animationPlayState = 'running';
      }
    });
  }, 100);
</script>
